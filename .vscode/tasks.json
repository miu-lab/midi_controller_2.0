{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Download Bitwig API",
            "type": "shell",
            "command": "${workspaceFolder}/extension/bitwig/download-api.sh",
            "windows": {
                "command": "${workspaceFolder}\\extension\\bitwig\\download-api.bat"
            },
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/extension/bitwig"
            }
        }
    ]
}